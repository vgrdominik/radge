<Window x:Class="RadgeScreen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RadgeScreen"
        mc:Ignorable="d"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{md:MaterialDesignFont}"
        Title="Radge Screen" Height="450" Width="800">
    <Grid>
        <md:Card Padding="32" Margin="16">
            <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}">
                My First Material Design App
                <Button x:Name="button" Click="button_Click" Width="187" Style="{DynamicResource MaterialDesignRaisedButton}">Click here to open Notepad Below</Button>
                <StackPanel x:Name="MainPanel" Height="257" Width="489" Margin="100,0,0,0"/>
            </TextBlock>
        </md:Card>

    </Grid>
</Window>
