<Window x:Class="RadgeScreen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RadgeScreen"
        mc:Ignorable="d"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{md:MaterialDesignFont}"
        Title="Radge Screen">
    <Grid>
        <md:Card Padding="5" Margin="10">
            <StackPanel x:Name="MainPanel" Margin="0" UseLayoutRounding="False">
                <Button x:Name="screenInitButton" Click="button_Click" Width="187" Style="{DynamicResource MaterialDesignRaisedButton}">Iniciar screen</Button>
            </StackPanel>
        </md:Card>

    </Grid>
</Window>
